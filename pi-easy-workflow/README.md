# pi-easy-workflow

Pi migration target for Easy Workflow.

Hard rule: this is only a folder inside the main repository.

Do not turn this directory into its own git repo.
Do not run `git init` here.
Do not create a `.git/` directory here.

Keep all work in this directory tracked by the parent repo so worktrees and merges stay visible to the workflow.
